** List of Python dependencies

import pandas as pd  # for handing dataframes
import numpy as np   # for handing array
import matplotlib.pyplot as plt  # for visualization
import seaborn as sns        # for visualizations

from sklearn.linear_model import LinearRegression   # for importing model 
from sklearn.model_selection import train_test_split # for dividing data into trian set and test set 
from sklearn.preprocessing import LabelEncoder     # for handing non-numeric values
from sklearn.metrics import mean_squared_error, mean_absolute_error, r2_score  # for evalution of model
